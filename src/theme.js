export const C = {
  bg: "#0b0f19",
  surface: "#111827",
  surface2: "#1f2937",
  accent: "#10b981",
  accentHover: "#059669",
  text: "#f3f4f6",
  textMuted: "#9ca3af",
  border: "#374151"
};
export const baseStyles = `* { box-sizing: border-box; margin: 0; padding: 0; } body { background-color: ${C.bg}; color: ${C.text}; }`;
